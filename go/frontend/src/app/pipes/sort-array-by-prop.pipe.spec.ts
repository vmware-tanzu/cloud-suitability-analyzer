import { SortArrayByPropPipe } from './sort-array-by-prop.pipe';

describe('SortArrayByPropPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrayByPropPipe();
    expect(pipe).toBeTruthy();
  });
});
